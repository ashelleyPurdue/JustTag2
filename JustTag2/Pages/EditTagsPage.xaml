<Page x:Class="JustTag2.Pages.EditTagsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:tp="clr-namespace:JustTag2.TagPallette"
      xmlns:local="clr-namespace:JustTag2.Pages"
      xmlns:p="clr-namespace:JustTag2.Previewers"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EditTagsPage">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="150*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="10"/>
                <Setter Property="Width" Value="50"/>
                <Setter Property="VerticalAlignment"
                            Value="Bottom"/>
            </Style>
        </Grid.Resources>

        <!-- Tag list -->
        <GroupBox
            Grid.Column="0"
            Grid.Row="1"
            Header="This file's tags">

            <TextBox x:Name="tagsTextbox"/>

        </GroupBox>

        <!-- Tag Pallette -->
        <GroupBox 
            Grid.Column="0" 
            Grid.Row="2"
            Header="Tag Pallette">

            <tp:TagPalletteView x:Name="tagPallette"/>

        </GroupBox>

        <!-- Previewer -->
        <p:MainPreviewer
            x:Name="previewer"
            Grid.Column="1"
            Grid.Row="1"
            Grid.RowSpan="2"/>

        <!-- OK/Cancel buttons -->
        <Button 
            Content="OK"
            HorizontalAlignment="Right"
            Grid.Column="1"
            Grid.Row="0"
            Click="OK_Click" Height="20" Margin="0,0,10,10"/>

        <Button 
            Content="Cancel"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Grid.Column="0"
            Grid.Row="0"
            Click="Cancel_Click" Height="20" Margin="10,10,0,0"/>

    </Grid>
</Page>
